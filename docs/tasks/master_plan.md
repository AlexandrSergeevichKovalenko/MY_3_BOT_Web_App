# Главный план: убрать костыли и построить точную систему

Живой документ. Ведётся с 16.08.2026, обновляется после каждой закрытой задачи.
Основа — аудит 92 находок (артефакт «Карта костылей»), свёрнутых в 7 корней.

**Зачем всё это.** Приложение должно давать человеку идеальный опыт: быстрый ответ,
чистый путь между вопросом и ответом, тяжёлое — в фоне, честные подсказки вместо
сырых ошибок. И ни одной выдуманной формы немецкого языка.

---

## Где мы сейчас

| группа | что это | состояние |
|---|---|---|
| **А. Приложение само выводит немецкий** | 14 находок: спряжение, склонение, степени сравнения, артикль | **закрыто на 95%** — формы берутся из справочника; остаток ведётся автоматом |
| **Б. Задание собирается механически** | 5 находок августовского списка | **замерено 21.08 — список НЕ подтвердился**, разбор ниже |
| **В. «Ошибка чтения = ноль / пусто / бесплатно»** | 20: лимиты, потолки, тариф, очередь ошибок | **замерено 18.08**; доказан 1 дефект (тариф), остальное — теория при 2–3 людях в сутки; поставлена диагностика, ждём данных |
| **Г. Отметка о выполнении ставится не по факту** | 9: планировщик, отчёты, картинки, транскрипты | ждёт |
| **Д. Ответ ученика уходит в никуда** | 5: спринт, тренажёр артиклей, «сохранено» | ждёт |
| **Е. Числа и цены продублированы на фронте** | 4: цена, таблица тарифов, «0 ⭐» | ждёт |
| **Ж. Техтекст в интерфейсе** | 5: стек JS на экране, коды ошибок в пузыре | ждёт |
| **З. ОДНА ДВЕРЬ СОХРАНЕНИЯ СЛОВА** | новая, поставлена владельцем 19.08.2026 | **следующая** |
| **Заморожено** | голосовой ассистент — 6 находок | не трогать до отдельного захода |

---

## Что уже закрыто по группе А

- формы существительных, прилагательных и наречий читаются из напечатанной таблицы
  de.wiktionary; арифметика удалена из движка;
- каскад: справочник → разбор составного слова по доказанному шву → модель с двумя
  совпавшими ответами → остаток владельцу;
- ночная работа идёт тем же путём, что разовая чистка (новое слово не повторяет историю);
- накопленное вычищено: 463 выдуманные таблицы, 47 негодных заголовков починено
  автоматически, 11 форм склеено с их словами, 36 слов подтверждено вторым источником;
- покрытие: существительные 97%, прилагательные 89%, наречия 94%.

**Осталось по А:** 14 кандидатов на удаление (ждут решения владельца), 6 настоящих
вопросов в очереди, и — главное — **вход не закрыт**: обрезанные слова вроде «Abschiebu»
попали в базу через какую-то дверь, и завтра придут новые. Это и есть группа З.

---

## Б. Замер 21.08.2026 — что оказалось на самом деле

Владелец потребовал мерить, а не пересказывать список: «в названных тобой интерактивах
у меня всё отлично». Замер это подтвердил. Записано здесь, чтобы старый список больше
никого не сбивал.

| что мерили | сколько | результат |
|---|---|---|
| wo-Fragen: весь банк | 1330 заданий, 132 набора | **0 расхождений** по шести проверкам |
| окончания прилагательных: основной генератор | 2340 выданных заданий | **0 расхождений** с таблицей склонения, все 27 клеток |
| окончания: резервный банк (пишет модель) | 30 выданных | 10 с кривым раскроем, 1 неверная русская подпись |
| ребус: сверка ответа | — | точное совпадение, **не подстрока** |

Три утверждения августовского списка оказались неверными:

- «дистракторы берутся из того же предложения» — нет. У wo-Fragen они из двух
  проверенных списков местоименных наречий плюс одна форма, выведенная из управления
  глагола; вторая правильная форма из вариантов **банится** специально;
- «ребус сверяет подстрокой» — нет, `answer_eval.check_rebus` сравнивает целиком;
  подстрока используется только при сборке, чтобы проверить вхождение части;
- «wo-Fragen собираются механически» — собираются детерминированно, из ручного банка
  169 записей, с восемью проверками готового задания и ночным карантином в 04:10.

**Что замер нашёл настоящего** — по резервному пути, который включается, только если
основной генератор не отдал задание. Пропуск стоял не на том слове:

    показано: ohne ein[___] gutes Argument   ответ «e»
    выйдет:   ohne eine gutes Argument       а верно: ohne ein gutes Argument

Закрыто 21.08.2026: страж на входе (`bot_3.py`, разбор ответа модели) и второй рубеж на
выдаче (`database.adjektiv_gap_rebuilds`); негодное задание снято с выдачи
(`scripts/adjektiv_retire_broken_gaps.py`); на таблицу окончаний поставлен тест
(`backend/tests/test_adjektiv_endings_table.py`) — до этого её не стерегло ничто.

**Урок для плана:** запись в списке находок — не факт о сегодняшнем состоянии.
Перед любой правкой раздел перемеряется на живых данных.

---

## З. Одна дверь сохранения слова (поставлена 19.08.2026)

Владелец: «Мы обязательно должны проверять ПЕРЕД сохранением… найти, откуда происходит
некорректное сохранение, проверить ВСЕ пути, свести их в единый механизм без костылей.
Сначала свериться со всеми справочниками, при недостатке — с моделью. Наша задача —
сохранить правильную версию слова или предложения: грамматически, лексически, с верной
заглавной буквой, с верной частью речи и обогащением.»

**Что входит в задачу:**

1. Найти ВСЕ пути, которыми слово/фраза попадает в базу (мини-апп, бот, ярлык, импорт,
   ночные работы, разборы). Составить карту с `file:line`.
2. Свести их в одну дверь. Правило: мимо двери в базу не попадает ничего.
3. Дверь проверяет по порядку: существует ли слово → правильное ли написание (умлауты,
   ß/ss, обрезка) → регистр по части речи (существительное с заглавной, глагол со
   строчной) → часть речи из справочника → формы → обогащение.
4. Источники по убыванию доверия: справочник → разбор составного слова → модель с двумя
   совпавшими ответами. Ничего не выдумывать, ничего не глушить.
5. Что дверь не смогла подтвердить — не выбрасывается молча, а идёт в разбор.

**Отдельная часть — разбор с ПОЛЬЗОВАТЕЛЕМ (не с админом):**

Раз в неделю или месяц человеку приходит в личку короткое сообщение: «вот слова, которые
ты сохранил, а мы не смогли подтвердить». К нему — чекбоксы. Отмеченные остаются,
неотмеченные удаляются везде: из его словаря, из общего пула, из всех таблиц.

Открытый вопрос владельца: как быть, когда слово человека устраивает, а перевод нет.
Обсудить до реализации.

---

## Как ведётся работа

Каждая задача — по замкнутому циклу из `CLAUDE.md` (9 шагов): анализ несколькими
агентами → чужие решения → обсуждение → чистка накопленного → перестройка системы →
автомат ночью → сомнения владельцу с кнопками → отчёт сам, без команды.

**После каждой закрытой задачи возвращаемся сюда и берём следующую группу.**
Углубление в отдельную задачу нормально, но план не теряется.
