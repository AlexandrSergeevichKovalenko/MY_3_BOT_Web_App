"""Мера качества карточек — она же заслон на входе.

Зачем этот модуль появился (владелец, 22.08.2026): «мне надоело вылавливать ошибки по
одной. Нужна рабочая система». Он прав, и разбор схемы показал три ошибки в моём подходе.

ОШИБКА ПЕРВАЯ. Я просил модель СОБЛЮДАТЬ двадцать требований в одном ответе, хотя
большинство из них проверяется механически, без всякого разбора грамматики: есть ли в
единице цифры, склеена ли она запятой, из закрытого ли списка помета, лежит ли форма из
текста внутри цитаты. Просьба к модели — это надежда. Проверка в коде — это гарантия.
Всё, что можно проверить, проверяется здесь, а модели остаётся то, где источника нет.

ОШИБКА ВТОРАЯ. Проверки были рассыпаны по генератору, судье и тестам, каждая со своим
счётчиком. Одно правило жило в трёх местах и в двух из них устаревало. Здесь они собраны
в ОДИН именованный список — добавить проверку теперь значит добавить строку в него.

ОШИБКА ТРЕТЬЯ. Качество мерили глаза владельца. Поэтому дефекты и находились по одному.
Тот же список работает мерой: прогнать по накопленным карточкам и получить число.

── Чего здесь НЕТ и почему ────────────────────────────────────────────────────
Здесь нет ни одной проверки, которая выводила бы немецкую грамматику своей арифметикой.
Род, возвратность, управление глагола, существует ли слово — на это есть справочники
(backend/article_authority.py, backend/german_word_gate.py), и спрашивать надо их, а не
догадываться регуляркой. Правило ноль: ответ берётся из источника.

Проверки ниже смотрят только на то, что видно в самой строке: цифры, запятые, длину,
принадлежность закрытому списку, вхождение одной строки в другую.
"""
from __future__ import annotations

import re

# Пометы формы — закрытый список. Всё остальное модель сочинила: «инфинитив с sich»,
# «Akkusativ», «Dativ Plural» — всё это приходило и всё это человеку читать нельзя.
ALLOWED_FORM_LABELS = {
    "словарная форма", "устойчивое выражение", "инфинитив",
    "именительный падеж", "винительный падеж", "дательный падеж",
    "родительный падеж", "множественное число", "повелительная форма",
}

# Служебные слова. Единица, состоящая ТОЛЬКО из них, — не единица: учить в ней нечего.
#
# Раньше по этому списку проверялся ХВОСТ («кончается служебным словом — значит обрезано»),
# и это было неверно, см. разбор у `_not_a_unit_at_all`. Теперь список отвечает на другой,
# честный вопрос: осталось ли в строке хоть одно содержательное слово.
_DANGLING_TAIL = {
    "der", "die", "das", "den", "dem", "des", "ein", "eine", "einen", "einem", "einer",
    "und", "oder", "aber", "dass", "weil", "wenn", "als", "sondern", "damit",
}

# Сколько слов может быть в языковой единице. «wie bestellt und nicht abgeholt» — пять,
# и это законная идиома. Восемь и больше — это уже кусок предложения.
MAX_UNIT_WORDS = 7


def _words(text: str) -> list:
    return re.findall(r"[A-Za-zÄÖÜäöüß]+", str(text or ""))


def fingerprint(text: str) -> str:
    """Только буквы и цифры в нижнем регистре. Нужен, чтобы сверять вхождение строк, не
    спотыкаясь о знаки препинания, которые в субтитрах стоят иначе, чем их перепишет
    модель."""
    return re.sub(r"[^0-9a-zäöüß]+", "", str(text or "").lower())


# ── Проверки. Каждая возвращает причину или пустую строку ──────────────────────

# ┌─ ПРОВЕРЕНО 27.08.2026. ЦИФРА В ЕДИНИЦЕ — НЕ ДЕФЕКТ. НЕ ВОЗВРАЩАТЬ ЭТУ ПРОВЕРКУ. ─┐
# │ Здесь стояла проверка `_has_digits`: любая единица с цифрой считалась «фактом из  │
# │ новости» и МОЛЧА удалялась — и на входе, и ночью из уже собранных выпусков.       │
# │                                                                                   │
# │ Повод разобраться: 27.08.2026 ночная проверка выбросила «über die 5% Hürde        │
# │ helfen» из выпуска по ролику ZDF, который называется «BSW im Wahlkampf: Zwischen  │
# │ Streit um AfD-Kurs und 5-Prozent-Hürde». Мы удалили термин, вынесенный редакцией  │
# │ в заголовок передачи.                                                             │
# │                                                                                   │
# │ Замер по живой базе (scratchpad-скрипт, только чтение): из 43 557 единиц общего   │
# │ словаря цифру содержат 484. Разложение: 441 предложение-пример (другая            │
# │ подсистема, правило их не касалось), 42 сочетания, 1 слово. Среди сочетаний —     │
# │ «Er ist total 0815», «Auf 180 sein», «die CO2-Abgabe», «50-Euro-Schein»,          │
# │ «5-Gänge-Menü», «Das war Mitte der 90er». Все законные, все были бы удалены.      │
# │                                                                                   │
# │ Второй половины — класса «мусор» — НЕТ. Владелец, 27.08.2026, разобрав кандидатов │
# │ в мусор поимённо: «Das wiegt 5 Kilogramm» учит глагол wiegen, «Thrombose mit 19»  │
# │ учит mit в значении возраста. «Не лезь вообще к цифрам, это нормальные            │
# │ предложения, не трогай их».                                                       │
# │                                                                                   │
# │ Признак «в строке есть цифра» не различает язык и число — это ровно та            │
# │ механическая догадка о немецком, которую запрещает правило ноль. Запрет снят и в  │
# │ промпте генератора (world_news_generator.py, раздел WAS EINE SPRACHEINHEIT IST).  │
# │ Как перемерить: посчитать в bt_3_lex_units единицы с `lemma ~ '[0-9]'` в разрезе  │
# │ kind и посмотреть не-предложения глазами.                                         │
# │ Тест, который не даст правилу вернуться:                                          │
# │ test_daily_video_rubric_alternation.py::test_a_digit_in_a_unit_is_not_a_defect    │
# └───────────────────────────────────────────────────────────────────────────────────┘


# ┌─ ПРОВЕРЕНО 27.08.2026. ХВОСТ И ЗАПЯТАЯ НЕ ГОВОРЯТ О НЕМЕЦКОМ НИЧЕГО. ────────────┐
# │ Здесь стояли две проверки, снятые вместе с проверкой цифр и по той же причине:    │
# │ обе судили немецкий по внешнему признаку, не зная РОЛИ слова.                     │
# │                                                                                   │
# │ `_dangling_tail` — «кончается артиклем или союзом, значит обрезано». Замер по      │
# │ 9841 немецкому слову и сочетанию живого словаря: 44 срабатывания, из них 34 на     │
# │ здоровых единицах, то есть ошибка в трёх случаях из четырёх.                      │
# │   • `ein` в конце — это НЕ артикль, а отделяемая приставка (9 случаев):           │
# │     «mir fällt etwas ein», «der Staat springt ein», «die Polizei setzt            │
# │     Wasserwerfer ein», «Das Fieber stellte sich ein». В новостях такой глагол     │
# │     на каждом шагу, и правило убивало их подряд.                                  │
# │   • `dass` в конце — речевая формула, а не обрыв (11): «es liegt nahe, dass»,     │
# │     «vorausgesetzt, dass», «wissenschaftliche Studien belegen, dass».             │
# │   • `der`/`die` после запятой — словарное оформление рода: «Nachhilfe, die».      │
# │   • `das` — указательное местоимение: «Kaum jemand weiß das».                     │
# │   • идиомы, кончающиеся союзом: «ohne Wenn und Aber», «Na und», «Entweder oder».  │
# │   • `als` в управлении глагола: «fungieren als», «beurteilen als».                │
# │                                                                                   │
# │ `_glued_by_comma` — «запятая и артикль = две склеенные единицы». 8 срабатываний,   │
# │ 4 на здоровых: `der/die/das` после запятой в немецком чаще всего значит «который»  │
# │ («Akku, der nicht nachlässt», «Dinge, die gleich sind») либо вводит множественное  │
# │ число («Vorzug, die Vorzüge», «Not, die Nöten»).                                  │
# │                                                                                   │
# │ Взамен ниже стоит `_not_a_unit_at_all` — вопрос, на который можно ответить без     │
# │ знания грамматики: осталось ли в строке хоть одно содержательное слово. На том же  │
# │ корпусе он ловит ровно 9 негодных («dass», «als», «weil», «eine», «des», «Aber»,   │
# │ «Wenn», «Sondern», «einer») и НИ ОДНОЙ здоровой. Мимо проходит один настоящий      │
# │ обрезок («Sicherheitsvorgaben und») — цена размена 34 убитых на 1 пропущенный.     │
# │ Как перемерить: прогнать признак по bt_3_lex_units, kind в ('word','collocation'). │
# │ Держит тест test_a_tail_word_does_not_make_a_unit_broken.                          │
# └───────────────────────────────────────────────────────────────────────────────────┘


def _not_a_unit_at_all(card, ctx):
    """В строке нет ни одного содержательного слова — учить нечего.

    Три признака, и ни один не про грамматику:
      • строка кончается запятой — это факт пунктуации, фразу вправду оборвали;
      • строка кончается запятой И ОДИНОКИМ АРТИКЛЕМ: «die Koalition auffordern, die» —
        случай владельца 22.08.2026, он видел это на экране. Признак узкий именно из-за
        артикля: с «dass» и «als» после запятой начинаются законные формулы («es liegt
        nahe, dass», «insofern, als»), а с артикля — ничего. Замер 27.08.2026 по 9842
        единицам словаря: 3 совпадения, и все три — словарная запись рода («Nachhilfe,
        die»), которой в карточке рубрики не место в любом случае: там артикль стоит
        ПЕРЕД словом;
      • все слова строки служебные («als», «weil», «Sondern») — тогда единицы просто нет.
    """
    de = str(card.get("de") or "").strip()
    if de.endswith(","):
        return "единица кончается запятой — обрезана посреди фразы"
    if re.search(r",\s*(der|die|das|den|dem|des|ein|eine|einen|einem|einer)$", de, re.I):
        return "единица кончается запятой и артиклем — обрезана посреди фразы"
    words = _words(de)
    if words and all(w.lower() in _DANGLING_TAIL for w in words):
        return "в единице одни служебные слова — учить нечего"
    return ""


def _too_long(card, ctx):
    """Слишком длинная единица — почти наверняка кусок предложения, а не оборот."""
    n = len(_words(card.get("de")))
    if n > MAX_UNIT_WORDS:
        return f"единица из {n} слов — это кусок предложения, а не оборот"
    return ""


def _bad_form_label(card, ctx):
    """Помета формы вне закрытого списка — значит модель её сочинила.

    ПУСТАЯ помета сюда НЕ входит, и это решение владельца 27.08.2026. Сочинённая помета
    («инфинитив с sich») — это неверные сведения на экране, их надо убирать. А ОТСУТСТВИЕ
    служебной подписи ничего не портит: немецкий верен, перевод верен, цитата сверена —
    выбрасывать такую карточку значит терять готовую работу из-за строчки, которую можно
    просто дозапросить (`missing_labels` ниже, дозапрос — daily_video_pack.fill_missing_labels).
    """
    form = str(card.get("form_ru") or "").strip().lower()
    if not form:
        return ""
    if form not in ALLOWED_FORM_LABELS:
        return f"помета формы не из списка: «{card.get('form_ru')}»"
    return ""


def _quote_not_in_transcript(card, ctx):
    """Цитата обязана дословно найтись в субтитрах — иначе она выдумана."""
    quote = str(card.get("quote_de") or "").strip()
    if not quote:
        return "нет цитаты из ролика"
    if fingerprint(quote) not in fingerprint(ctx.get("transcript") or ""):
        return "цитаты нет в субтитрах ролика"
    return ""


def _text_form_not_in_quote(card, ctx):
    """Форма из текста обязана лежать внутри цитаты — иначе она тоже выдумана."""
    in_text = str(card.get("de_in_text") or "").strip()
    if not in_text:
        return "нет формы из текста"
    if fingerprint(in_text) not in fingerprint(card.get("quote_de")):
        return "формы из текста нет в цитате"
    return ""


def _neutral_in_standup(card, ctx):
    """Нейтральное бытовое слово в рубрике сленга — добор до количества.

    Пустая помета регистра, как и пустая помета формы, карточку больше не убивает: её
    дозапрашивают. Убирается только НАЗВАННОЕ нейтральным — это ответ модели, а не пробел.
    """
    if not ctx.get("requires_register"):
        return ""
    register = str(card.get("register_ru") or "").strip()
    if not register:
        return ""
    if register.lower().startswith("нейтральн"):
        return "нейтральное слово в рубрике живой речи"
    return ""


def _empty_fields(card, ctx):
    for field, label in (("de", "единицы"), ("translation_ru", "перевода"),
                         ("usage_ru", "подсказки об употреблении")):
        if not str(card.get(field) or "").strip():
            return f"нет {label}"
    return ""


# Порядок важен: сперва то, из-за чего карточка бессмысленна, потом частности.
CARD_CHECKS = (
    ("пустое поле", _empty_fields),
    ("цитата выдумана", _quote_not_in_transcript),
    ("форма из текста выдумана", _text_form_not_in_quote),
    # Снято 27.08.2026, разбор в тексте файла выше: «цифры вместо единицы»,
    # «склейка запятой», «кончается служебным словом».
    ("не единица", _not_a_unit_at_all),
    ("кусок предложения", _too_long),
    ("сочинённая помета формы", _bad_form_label),
    ("нейтральное слово", _neutral_in_standup),
)


def missing_labels(card: dict, *, requires_register: bool = False) -> list:
    """Каких служебных помет на карточке НЕ ХВАТАЕТ. Это не претензия к карточке, а
    задание на дозапрос: помета — единственное, чего у нас нет, и она добирается вопросом
    к модели, а не удалением готовой работы (решение владельца 27.08.2026).

    Пустой список — добирать нечего.
    """
    gaps = []
    if not str(card.get("form_ru") or "").strip():
        gaps.append("form_ru")
    if requires_register and not str(card.get("register_ru") or "").strip():
        gaps.append("register_ru")
    return gaps


def cards_missing_labels(cards: list, *, requires_register: bool = False) -> list:
    """Номера карточек, которым нужен дозапрос помет. Считается ВСЕГДА — молчащий пробел
    неотличим от отсутствующего, а владелец обязан видеть числом, сколько мы не знаем."""
    return [i for i, card in enumerate(cards or [])
            if missing_labels(card, requires_register=requires_register)]


def check_card(card: dict, *, transcript: str = "", requires_register: bool = False) -> list:
    """Все претензии к карточке. Пустой список — карточка чистая."""
    ctx = {"transcript": transcript, "requires_register": requires_register}
    found = []
    for name, check in CARD_CHECKS:
        why = check(card, ctx)
        if why:
            found.append((name, why))
    return found


def check_cards(cards: list, *, transcript: str = "", requires_register: bool = False) -> dict:
    """Мера по пачке карточек: сколько чистых, сколько с какой бедой.

    Возвращает и сами претензии по каждой карточке — чтобы владельцу можно было показать
    не только число, но и что именно не так.
    """
    clean, flagged, by_kind = 0, [], {}
    for card in cards or []:
        problems = check_card(card, transcript=transcript, requires_register=requires_register)
        if not problems:
            clean += 1
            continue
        flagged.append({"de": card.get("de"), "problems": problems})
        for name, _ in problems:
            by_kind[name] = by_kind.get(name, 0) + 1
    total = len(cards or [])
    return {
        "total": total,
        "clean": clean,
        "flagged": len(flagged),
        "share_clean": round(clean / total, 2) if total else 0.0,
        "by_kind": dict(sorted(by_kind.items(), key=lambda kv: -kv[1])),
        "cards": flagged,
    }


# ── Сверка со справочником: артикль берётся из источника, а не у модели ────────
#
# Правило ноль: ответ берётся из источника, модель только читает. Род существительного —
# ровно тот случай, где источник есть и давно построен: backend/article_authority.py
# (собственный справочник + Wiktionary). Спрашивать род у модели, когда есть справочник,
# значит сознательно предпочесть догадку знанию.
#
# Почему сюда можно ходить в сеть, а на сохранение слова — нельзя: рубрика готовится
# фоновой работой раз в день и никого не заставляет ждать. Живой путь сохранения такого
# позволить не может, и там справочник спрашивают только из прогретой памяти.

_ARTICLES = ("der", "die", "das")

# Неопределённый артикль в заголовке — НЕ словарная форма, но такое же доказательство
# того, что перед нами существительное, как и определённый.
#
# До 24.08.2026 `split_article` принимала только der/die/das, и карточка, пришедшая из
# колоды как «eine Schnapsidee», для заслона не существовала вовсе: он её не проверял,
# не исправлял и не заносил в отчёт. Дальше дверь записи снимала «eine» (правильно —
# это не словарный артикль), а определённый никто не ставил, и слово уезжало человеку
# голым. Владелец увидел это на карточке «Schnapsidee» 23.08.2026.
#
# Живой замер того же дня: в невыданном ещё выпуске на 24.08 таких карточек было 3 из 10
# («eine Schnapsidee», «eine Dauerkarte», «eine Wildcard»); среди 356 сохранений из
# колоды за три месяца — 101 карточка пришла правильно, «der/die/das + слово».
#
# Оборотов это не касается и коснуться не может: ниже стоит `len(parts) != 2`, поэтому
# «einen Kater haben» и «eine Pressekonferenz abhalten» сюда не попадают. Артикль
# приклеивается ТОЛЬКО к одиночному существительному — правило владельца от 22.08.2026,
# см. backend/tests/test_article_belongs_to_a_word_not_a_phrase.py.
_INDEFINITE_ARTICLES = ("ein", "eine", "einen", "einem", "einer", "eines")


def is_indefinite_article(article: str) -> bool:
    """Неопределённый ли это артикль. Нужен там, где важно не «какой род», а «форма
    не словарная»: род по «eine» не определяется — женский и средний дают одну и ту же
    форму («eine Idee», но «ein Buch» и «ein Mann» тоже совпадают)."""
    return str(article or "").strip().lower() in _INDEFINITE_ARTICLES


def split_article(de: str) -> tuple:
    """Разделить «die Kommentarspalte» на («die», «Kommentarspalte»).

    Понимает и определённый артикль, и неопределённый: «eine Schnapsidee» →
    («eine», «Schnapsidee»). Что с этим делать — решают вызывающие: определённый
    сверяется со справочником, неопределённый ЗАМЕНЯЕТСЯ на определённый из справочника.

    Возвращает (None, None), если это не одиночное существительное с артиклем: у оборотов
    и глаголов рода нет, и сверять там нечего.
    """
    parts = str(de or "").strip().split()
    if len(parts) != 2:
        return (None, None)
    article, word = parts[0].lower(), parts[1]
    # Заглавная буква НЕ требуется: артикль уже сказал, что это существительное, а
    # неверный регистр — как раз то, что мы пришли чинить. Требовать заглавную здесь
    # значило бы исключить ровно те карточки, ради которых сверка и делается
    # (поймано на «die kommentarspalte» 22.08.2026).
    if article not in _ARTICLES and not is_indefinite_article(article):
        return (None, None)
    if not re.fullmatch(r"[A-Za-zÄÖÜäöüß-]{2,}", word):
        return (None, None)
    return (article, word)


def article_from_reference(de: str, *, allow_network: bool = False) -> tuple:
    """Что о роде этого слова говорит справочник.

    Возвращает (артикль, откуда) или (None, причина). None означает «справочник не знает» —
    и это НЕ повод что-то подставлять: неизвестность честнее догадки.
    """
    article, word = split_article(de)
    if not word:
        return (None, "не одиночное существительное — рода нет")
    try:
        from backend.article_authority import authoritative_article
        found, source = authoritative_article(word, allow_network=allow_network)
    except Exception:
        return (None, "справочник недоступен")
    if found:
        return (found.lower(), source)
    # Вторая ступень: НАПЕЧАТАННАЯ таблица склонения. 23.08.2026 их стало 89 704 вместо
    # 2 909, и там род уже проставлен в самом именительном падеже («die Ratte»). Арбитр
    # `authoritative_article` про эту выгрузку пока не знает — замер 24.08.2026 на пяти
    # застрявших словах: Kurzbefehl, Bugfahrwerk, Kostenhochlaufkurve и Wildcard арбитр
    # не знает, а таблица склонений даёт der, das, die и die соответственно.
    # Ходить сюда дёшево (свой Postgres, без сети), поэтому спрашиваем всегда.
    try:
        from backend.noun_declension_reference import article_from_declension_reference
        found, source = article_from_declension_reference(word)
    except Exception:
        return (None, f"справочник не знает слова «{word}»")
    if found:
        return (found.lower(), source)
    return (None, f"справочник не знает слова «{word}»")


def article_disagrees_with_reference(card: dict, *, allow_network: bool = False) -> str:
    """Расходится ли артикль на карточке со справочником.

    Пустая строка — расхождения нет ИЛИ справочник промолчал. Молчание справочника не
    делает карточку виноватой: мы просто не знаем, и придумывать не станем.
    """
    ours, word = split_article(card.get("de"))
    if not ours:
        return ""
    theirs, source = article_from_reference(card.get("de"), allow_network=allow_network)
    if is_indefinite_article(ours):
        # Неопределённый артикль — это не «расхождение о роде», а неверная ФОРМА
        # заголовка: в словаре слово стоит с определённым. Претензия справедлива и
        # тогда, когда справочник промолчал, — иначе «eine Wildcard» ушла бы человеку
        # молча, а молчание тут запрещено ровно так же, как выдумка.
        if theirs:
            return (f"в заголовке неопределённый артикль: «{ours} {word}» — "
                    f"словарная форма «{theirs} {word}» ({source})")
        return (f"в заголовке неопределённый артикль: «{ours} {word}» — "
                f"словарная форма требует der/die/das, а справочник рода не знает")
    if not theirs or theirs == ours:
        return ""
    return (f"артикль расходится со справочником: у нас «{ours} {word}», "
            f"источник даёт «{theirs}» ({source})")


def correct_article_from_reference(card: dict, *, allow_network: bool = False) -> tuple:
    """Поправить артикль по справочнику. Возвращает (карточка, что сделали).

    Это НЕ выдумывание: новый артикль берётся из источника, названного по имени. Если
    источник молчит — карточка возвращается нетронутой. Молчание не повод угадывать.
    """
    ours, word = split_article(card.get("de"))
    if not ours:
        return (card, "")
    theirs, source = article_from_reference(card.get("de"), allow_network=allow_network)
    if not theirs:
        # Справочник молчит. Подставлять нечего — но и промолчать нельзя, если в
        # заголовке стоит неопределённый артикль: это заведомо не словарная форма, и
        # владелец обязан увидеть такую карточку числом, а не найти её глазами на экране.
        if is_indefinite_article(ours):
            return (card, f"НЕ ИСПРАВЛЕНО, нужен род: «{ours} {word}» — "
                          f"неопределённый артикль, справочник слова не знает")
        return (card, "")
    if theirs == ours:
        return (card, "")
    fixed = dict(card)
    fixed["de"] = f"{theirs} {word}"
    if is_indefinite_article(ours):
        return (fixed, f"заголовок приведён к словарной форме по справочнику ({source}): "
                       f"«{ours} {word}» → «{theirs} {word}»")
    return (fixed, f"артикль исправлен по справочнику ({source}): «{ours}» → «{theirs}»")


# ── Существование и написание слова — тоже из справочника ─────────────────────
#
# Второй незакрытый резерв достоверности. `backend/german_word_gate.py` умеет две вещи,
# которые я до сих пор просил у модели:
#   • существует ли такое немецкое слово вообще (ходит в DWDS и второй справочник);
#   • как оно пишется — включая заглавную букву у существительных.
#
# Именно этим закрывается дефект «herzinfarkt bekommen» со строчной буквы, который
# владелец увидел 22.08.2026: справочник поднимает заглавную сам, и просить об этом
# модель больше не нужно. Просьба — надежда, справочник — знание.
#
# Модель здесь НЕ спрашивается (allow_model=False): нам нужен источник, а не вторая
# догадка поверх первой.

def _single_word_of(de: str) -> str:
    """Одно слово из заголовка — с артиклем или без. Для оборотов возвращает пустую
    строку: дверь слова разбирает ОДНО слово, многословное до неё не доходит."""
    text = re.sub(r"\(.*?\)", " ", str(de or "")).strip()
    parts = text.split()
    if len(parts) == 2 and parts[0].lower() in _ARTICLES:
        parts = parts[1:]
    if len(parts) != 1:
        return ""
    word = parts[0].strip(".,!?»«\"'")
    return word if re.fullmatch(r"[A-Za-zÄÖÜäöüß-]{2,}", word) else ""


def spelling_from_reference(de: str, *, allow_network: bool = False) -> tuple:
    """Что справочник говорит о написании и существовании слова.

    Возвращает (статус, исправленное_написание). Статус «неизвестно» — справочник не
    ответил; это НЕ повод ни выбрасывать карточку, ни что-то подставлять.
    """
    word = _single_word_of(de)
    if not word:
        return ("не одиночное слово", "")
    try:
        from backend.german_word_gate import CONFIRMED, NOT_A_WORD, REPAIRED, check_word
        verdict = check_word(word, allow_network=allow_network, allow_model=False)
    except Exception:
        return ("справочник недоступен", "")
    status = str(verdict.get("status") or "")
    fixed = str(verdict.get("text") or "").strip()
    if status == NOT_A_WORD:
        return ("не слово", "")
    if status == REPAIRED and fixed and fixed != word:
        return ("исправлено", fixed)
    if status == CONFIRMED:
        return ("подтверждено", fixed or word)
    return ("не подтверждено", "")


def correct_spelling_from_reference(card: dict, *, allow_network: bool = False) -> tuple:
    """Поправить написание заголовка по справочнику. Возвращает (карточка, что сделали).

    Правится ТОЛЬКО само слово, артикль остаётся на месте — им занимается сверка рода.
    Слово, которого справочник не знает, не трогается: у стендапа это сплошь сленг, и
    решение владельца 22.08.2026 — верить карточке, а не справочнику, когда речь живая.
    """
    # ТОЛЬКО существительное с артиклем. Проверка 22.08.2026 показала, почему это важно:
    # на «heulen» справочник отвечает «Heulen» с заглавной — и он прав со своей стороны,
    # «das Heulen» существует как отглагольное существительное. Но на карточке «heulen» —
    # это ГЛАГОЛ «рыдать», и подставив заглавную, мы своими руками сделали бы верную
    # карточку неверной.
    #
    # Справочник отвечает на вопрос «как пишется это слово в роли, которую я для него
    # выбрал», а нам нужна роль, в которой слово стоит на карточке. Применять ответ
    # источника к другому вопросу — то же выдумывание, только чужими руками.
    #
    # Артикль на карточке роль называет однозначно: это существительное. Без артикля роль
    # неизвестна, и написание не трогаем.
    article, _noun = split_article(card.get("de"))
    if not article:
        return (card, "")
    word = _single_word_of(card.get("de"))
    if not word:
        return (card, "")
    status, fixed = spelling_from_reference(card.get("de"), allow_network=allow_network)
    if status != "исправлено" or not fixed:
        return (card, "")
    updated = dict(card)
    updated["de"] = str(card.get("de") or "").replace(word, fixed, 1)
    return (updated, f"написание исправлено по справочнику: «{word}» → «{fixed}»")


def word_not_german(card: dict, *, allow_network: bool = False) -> str:
    """Справочник прямо говорит, что такого слова нет. Тогда карточке не место на экране.

    «Не подтверждено» сюда НЕ попадает: справочники плохо знают сленг, а стендап — это
    сплошь сленг. Выбрасываем только то, что источник назвал не словом.
    """
    # Живую речь справочники знают плохо, а стендап — это сплошь сленг. Решение владельца
    # 22.08.2026: карточке со сленговой пометой верим больше, чем справочнику. Поэтому
    # помеченную живую речь на существование не проверяем вовсе.
    register = str(card.get("register_ru") or "").strip().lower()
    if register and not register.startswith("нейтральн"):
        return ""
    status, _ = spelling_from_reference(card.get("de"), allow_network=allow_network)
    if status == "не слово":
        return f"справочник не знает такого слова: «{card.get('de')}»"
    return ""


# ── Ночная перепроверка того, что УЖЕ лежит в базе ────────────────────────────
#
# Заслон стоит на ВХОДЕ: он смотрит свежую карточку и не пускает негодную дальше. Но
# карточка, уже лежащая в базе, через него больше никогда не проходит — а значит:
#   • всё, собранное до появления проверки, остаётся с прежними дефектами;
#   • любая НОВАЯ проверка не применяется к накопленному.
#
# 23.08.2026 владелец увидел на экране «ein neuer Markt, der Graumarkt» — карточку, которую
# заслон уже умел ловить, но она собралась раньше. Я почистил это РУКАМИ, и владелец
# справедливо спросил: «а в будущем как? опять руками?»
#
# Поэтому перепроверка стала ночной работой. Правило проекта: повторяющаяся работа идёт на
# автомат, а не в список задач.
#
# ВАЖНО про субтитры. Часть проверок сверяет карточку с текстом ролика, а субтитры прошлых
# дней ночная чистка стирает. Без текста эти проверки дали бы «цитаты нет в субтитрах» на
# КАЖДОЙ карточке и вычистили бы всё подчистую. Поэтому при отсутствии текста они
# пропускаются: отсутствие источника — не улика против карточки.

_CHECKS_NEEDING_TRANSCRIPT = {"цитата выдумана", "форма из текста выдумана"}


def recheck_cards(cards: list, *, transcript: str = "", requires_register: bool = False) -> dict:
    """Перепроверить готовые карточки. Возвращает что оставить, что убрать и почему.

    Нет текста ролика — проверки, которым он нужен, пропускаются. Судить карточку по
    источнику, которого у нас больше нет, нельзя.
    """
    has_transcript = bool(str(transcript or "").strip())
    keep, removed = [], []
    for card in cards or []:
        problems = [
            (name, why)
            for name, why in check_card(card, transcript=transcript,
                                        requires_register=requires_register)
            if has_transcript or name not in _CHECKS_NEEDING_TRANSCRIPT
        ]
        if problems:
            removed.append({"de": card.get("de"), "why": problems[0][1]})
        else:
            keep.append(card)
    return {
        "keep": keep,
        "removed": removed,
        "checked_against_transcript": has_transcript,
    }
