---
description: Продолжить задачу «ярлык и словарь на четырёх языках» (немецкий, английский, испанский, итальянский)
---

Задача владельца, законсервирована 10.08.2026 после этапа 0. Ветка — `agent/mehrsprachig`,
worktree заводится командой `./agent-worktree.sh mehrsprachig`.

Сделай по порядку:

1. Прочитай стратегию целиком: `docs/tasks/shortcut_multilang_strategy.md`.
2. Прочитай отчёт этапа 0: `docs/tasks/shortcut_multilang_senses_audit.md` — раздел
   «Где мы остановились» в стратегии говорит, что уже починено на живой базе.
3. Перепроверь, что рубильник по-прежнему на месте: `GERMAN_ONLY_MODE` в
   `backend/backend_server.py` (дефолт `"1"`, в проде переменная не задана) и
   `const GERMAN_ONLY_MODE = true` в `frontend/src/App.jsx`. **Снимать его — не наша
   работа**, это шаг переделки словаря (`/slovar`).
4. Скажи владельцу коротко: на каком этапе стоим, что изменилось в коде с момента
   консервации, и с чего предлагаешь продолжить.

Главное правило этой задачи: цепочка строится так, чтобы **при включённом
`GERMAN_ONLY_MODE` поведение не менялось ни на байт**, а в день, когда флаг снимут,
английский заработал сам — без правки ярлыка на телефоне и без действий пользователя.

Ничего в коде приложения не менять, пока владелец не подтвердит, что пора.
